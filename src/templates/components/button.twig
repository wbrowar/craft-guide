{% extends 'guide/_partials/render_component.twig' %}

{% set attributes = {
  href: url ?? null,
  rel: target ?? true ? "noopener" : null,
  target: target ?? true,
} %}

{% set classes = ['btn'] %}
{% set element = (url ?? false) ? 'a' : 'button' %}

{% block content -%}{{ label }}{%- endblock %}