{% extends 'guide/_partials/render_component.twig' %}

{% set attributes = {
  href: url ?? null,
  rel: target ?? false ? "noopener" : null,
  target: target ?? false,
} %}

{% set classes = ['btn'] %}
{% set element = (attributes.href ?? false) ? 'a' : 'button' %}

{% block content -%}{{ label }}{%- endblock %}